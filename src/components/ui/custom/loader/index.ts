// Componentes principais
export { Loader, SimpleLoader } from "./loader";

// Types
export type { LoaderProps, LoaderConfig } from "./types";

// Re-export para compatibilidade
export { Loader as default } from "./loader";
